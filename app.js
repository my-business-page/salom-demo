const loader=document.querySelector('#loader');const line=document.querySelector('.loader__line span');const canvas=document.querySelector('#webgl');
let scene,camera,renderer,particles,orb;function init3D(){scene=new THREE.Scene();camera=new THREE.PerspectiveCamera(55,innerWidth/innerHeight,.1,100);camera.position.z=6;renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.8));renderer.setSize(innerWidth,innerHeight);const geo=new THREE.BufferGeometry();const count=1800;const pos=new Float32Array(count*3);for(let i=0;i<count;i++){const r=5*Math.pow(Math.random(),.7),a=Math.random()*Math.PI*2;pos[i*3]=Math.cos(a)*r;pos[i*3+1]=(Math.random()-.5)*5;pos[i*3+2]=Math.sin(a)*r-2}geo.setAttribute('position',new THREE.BufferAttribute(pos,3));const mat=new THREE.PointsMaterial({color:0xd6a65d,size:.025,transparent:true,opacity:.7,blending:THREE.AdditiveBlending});particles=new THREE.Points(geo,mat);scene.add(particles);const g=new THREE.IcosahedronGeometry(1.7,5);const m=new THREE.MeshBasicMaterial({color:0x8b3e4e,wireframe:true,transparent:true,opacity:.12});orb=new THREE.Mesh(g,m);scene.add(orb);animate()}function animate(){requestAnimationFrame(animate);particles.rotation.y+=.00045;particles.rotation.x+=.00015;orb.rotation.x+=.001;orb.rotation.y-=.0007;renderer.render(scene,camera)}init3D();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});
window.addEventListener('load',()=>{gsap.to(line,{width:'100%',duration:1.4,ease:'power2.inOut'});gsap.to(loader,{autoAlpha:0,duration:.8,delay:1.25,onComplete:()=>loader.remove()});gsap.utils.toArray('.reveal').forEach((el,i)=>gsap.to(el,{opacity:1,y:0,duration:1,delay:1.5+i*.04,scrollTrigger:{trigger:el,start:'top 88%'}}))});
if(window.Lenis){const lenis=new Lenis({duration:1.15,smoothWheel:true});function raf(t){lenis.raf(t);requestAnimationFrame(raf)}requestAnimationFrame(raf)}
const glow=document.querySelector('.cursor-glow');addEventListener('pointermove',e=>{if(glow)gsap.to(glow,{x:e.clientX,y:e.clientY,duration:.35,ease:'power2.out'});document.querySelectorAll('[data-parallax]').forEach(el=>{const p=+el.dataset.parallax;gsap.to(el,{x:(e.clientX-innerWidth/2)*p,y:(e.clientY-innerHeight/2)*p,duration:.7})})});
document.querySelectorAll('.tilt').forEach(card=>card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;gsap.to(card,{rotateY:x*7,rotateX:-y*7,duration:.35,transformPerspective:800})}));document.querySelectorAll('.tilt').forEach(card=>card.addEventListener('pointerleave',()=>gsap.to(card,{rotateY:0,rotateX:0,duration:.7})));document.querySelectorAll('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();gsap.to(el,{x:(e.clientX-r.left-r.width/2)*.15,y:(e.clientY-r.top-r.height/2)*.15,duration:.3})});el.addEventListener('pointerleave',()=>gsap.to(el,{x:0,y:0,duration:.5}))});
document.querySelectorAll('[data-count]').forEach(el=>{ScrollTrigger.create({trigger:el,start:'top 90%',once:true,onEnter:()=>{const n=+el.dataset.count;gsap.fromTo(el,{innerText:0},{innerText:n,duration:1.6,ease:'power2.out',snap:{innerText:1}})}})});
let audioCtx,osc;document.querySelector('#soundToggle')?.addEventListener('click',e=>{if(!audioCtx){audioCtx=new AudioContext();osc=audioCtx.createOscillator();const gain=audioCtx.createGain();osc.frequency.value=110;gain.gain.value=.008;osc.connect(gain).connect(audioCtx.destination);osc.start();e.currentTarget.innerHTML='Sound on <span>◉</span>'}else if(audioCtx.state==='running'){audioCtx.suspend();e.currentTarget.innerHTML='Sound off <span>◉</span>'}else{audioCtx.resume();e.currentTarget.innerHTML='Sound on <span>◉</span>'}});