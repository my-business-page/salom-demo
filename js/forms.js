export function initForms(){document.querySelectorAll('form[data-booking],form[data-event-form]').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(f));localStorage.setItem('zaara-demo-enquiry',JSON.stringify(data));f.innerHTML='<div class="success"><span class="kicker">Received</span><h2>Your request is staged.</h2><p>This is a demo. Connect the form to your reservation/CRM provider before production.</p><a class="gold-btn" href="index.html">Back to ZAARA</a></div>'}))}