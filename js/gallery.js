export async function initGallery(){const g=document.querySelector('#gallery-grid');if(!g)return;const dishes=await fetch('data/dishes.json').then(r=>r.json());g.innerHTML=dishes.slice(0,18).map((d,i)=>`<figure class="gallery-item"><img data-food="${d.imageQuery}" alt="${d.name}" loading="lazy"><figcaption>${String(i+1).padStart(2,'0')} · ${d.name}</figcaption></figure>`).join('');import('./image-loader.js').then(x=>x.resolveImages(g))}