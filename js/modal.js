export function openDish(d){const m=document.querySelector('#dish-modal');if(!m)return;m.innerHTML=`<div class="modal-backdrop"></div><article class="dish-modal"><button class="modal-x" aria-label="Close">×</button><img data-food="${d.imageQuery}" alt="${d.name}"><div><span class="kicker">${d.region}</span><h2>${d.name}</h2><p>${d.description}</p><p>${d.tags?.map(x=>`<b>${x}</b>`).join(' · ')||''}</p><strong>₹${d.price}</strong><button class="gold-btn" data-order>Order this dish</button></div></article>`;m.className='modal-root show';m.querySelector('.modal-x').onclick=()=>m.className='modal-root';m.querySelector('.modal-backdrop').onclick=()=>m.className='modal-root';const img=m.querySelector('img');import('./image-loader.js').then(x=>x.loadImage(img));m.querySelector('[data-order]').onclick=()=>{location.href=`reserve.html?dish=${encodeURIComponent(d.name)}`}}