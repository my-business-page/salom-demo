export function initVfx(){const c=document.querySelector('#vfx');if(!c||matchMedia('(prefers-reduced-motion: reduce)').matches)return;const x=c.getContext('2d');let w,h,dpr=Math.min(devicePixelRatio||1,1.5),pts=[];function size(){w=innerWidth;h=innerHeight;c.width=w*dpr;c.height=h*dpr;x.setTransform(dpr,0,0,dpr,0,0);pts=Array.from({length:Math.min(70,Math.floor(w/14))},()=>({x:Math.random()*w,y:Math.random()*h,s:.5+Math.random()*1.5,v:.08+Math.random()*.25}))}function loop(){x.clearRect(0,0,w,h);for(const p of pts){p.y-=p.v;if(p.y<0)p.y=h;x.fillStyle='rgba(222,176,92,.45)';x.fillRect(p.x,p.y,p.s,p.s)}requestAnimationFrame(loop)}addEventListener('resize',size,{passive:true});size();loop()}