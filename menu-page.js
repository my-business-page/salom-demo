const filters=document.querySelector('#filters'),grid=document.querySelector('#menuGrid');const cats=['All','North','South','East','West','Street','Dessert','Drinks'];let active='All';cats.forEach(c=>{const b=document.createElement('button');b.className='filter'+(c==='All'?' active':'');b.textContent=c;b.onclick=()=>{active=c;document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');render()};filters.appendChild(b)});function render(){grid.innerHTML='';menu.filter(d=>active==='All'||d.region===active).forEach(d=>{const el=document.createElement('article');el.className='dish-card tilt';el.innerHTML=`<div class="dish-photo"><img loading="lazy" src="${d.img}" alt="${d.name}"></div><div class="dish-info"><h3>${d.name}</h3><p>${d.region} · signature interpretation</p><div class="price">₹${d.price}</div></div>`;el.onclick=()=>location.href=`reserve.html?dish=${encodeURIComponent(d.name)}`;grid.appendChild(el)})}render();