const reduce=matchMedia('(prefers-reduced-motion: reduce)');const lowPower=navigator.hardwareConcurrency&&navigator.hardwareConcurrency<=4;document.documentElement.classList.toggle('low-power',lowPower);function rafThrottle(fn){let ticking=false;return(...args)=>{if(ticking)return;ticking=true;requestAnimationFrame(()=>{ticking=false;fn(...args)})}}const updatePointer=rafThrottle(e=>{document.documentElement.style.setProperty('--mx',`${(e.clientX/innerWidth-.5)*40}px`);document.documentElement.style.setProperty('--my',`${(e.clientY/innerHeight-.5)*40}px`)});if(!reduce.matches)addEventListener('pointermove',updatePointer,{passive:true});document.addEventListener('visibilitychange',()=>document.documentElement.classList.toggle('page-hidden',document.hidden));