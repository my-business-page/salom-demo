const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const fine=matchMedia('(pointer:fine)').matches;if(!reduce&&fine){let tx=0,ty=0,cx=0,cy=0;addEventListener('pointermove',e=>{tx=e.clientX;ty=e.clientY},{passive:true});function loop(){cx+=(tx-cx)*.12;cy+=(ty-cy)*.12;document.documentElement.style.setProperty('--cursor-x',cx+'px');document.documentElement.style.setProperty('--cursor-y',cy+'px');requestAnimationFrame(loop)}loop()}document.querySelectorAll('a,button').forEach(el=>el.addEventListener('click',()=>{el.animate([{transform:'scale(1)'},{transform:'scale(.96)'},{transform:'scale(1)'}],{duration:220,easing:'ease-out'})}));