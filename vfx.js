// ZAARA ambient VFX layer: particles, pointer glow, tilt and kinetic scroll
const c=document.createElement('canvas');c.id='vfx';document.body.prepend(c);const x=c.getContext('2d');let p=[],mx=-999,my=-999;function size(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';x.scale(devicePixelRatio,devicePixelRatio)}function seed(){p=Array.from({length:Math.min(140,Math.floor(innerWidth/6))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:.5+Math.random()*2,v:.15+Math.random()*.55,a:.15+Math.random()*.45}))}function loop(){x.clearRect(0,0,innerWidth,innerHeight);for(const q of p){q.y-=q.v;if(q.y<0)q.y=innerHeight;const dx=mx-q.x,dy=my-q.y,d=Math.hypot(dx,dy)||1;if(d<130){q.x-=dx/d*1.4;q.y-=dy/d*1.4}x.beginPath();x.arc(q.x,q.y,q.r,0,Math.PI*2);x.fillStyle=`rgba(222,174,90,${q.a})`;x.fill()}requestAnimationFrame(loop)}addEventListener('resize',()=>{size();seed()});addEventListener('pointermove',e=>{mx=e.clientX;my=e.clientY});size();seed();loop();